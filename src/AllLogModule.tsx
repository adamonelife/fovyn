import {useEffect,useMemo,useState} from 'react';
import {Activity,ChevronRight,Plus,Search,X} from 'lucide-react';
import {addMetricRecord} from './metricsRepository';
import {loadTrackers,type Tracker,type TrackerOptions} from './trackerRepository';
const localNow=()=>{const now=new Date(Date.now()-new Date().getTimezoneOffset()*60000);return now.toISOString().slice(0,16)};
const expected=(tracker:Tracker)=>tracker.schedule?.frequency_type==='daily'||(tracker.schedule?.frequency_type==='specific_days'&&tracker.schedule.days_of_week.includes(new Date().getDay()));
function Recorder({tracker,unit,close,saved}:{tracker:Tracker;unit:string;close:()=>void;saved:()=>void}){const[value,setValue]=useState(''),[occurred,setOccurred]=useState(localNow()),[note,setNote]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);return <div className="sheet-shade" onMouseDown={close}><section className="metric-recorder" onMouseDown={e=>e.stopPropagation()}><button className="sheet-close" onClick={close} aria-label="Close"><X/></button><p className="eyebrow">LOG</p><h2>{tracker.name}</h2><label>Value<div className="metric-value"><input autoFocus type="number" step="any" value={value} onChange={e=>setValue(e.target.value)}/><span>{unit}</span></div></label><label>When<input type="datetime-local" value={occurred} onChange={e=>setOccurred(e.target.value)}/></label><label>Note (optional)<textarea value={note} onChange={e=>setNote(e.target.value)}/></label>{error&&<p className="goal-error">{error}</p>}<button className="save-record" disabled={busy||value===''||!Number.isFinite(Number(value))} onClick={async()=>{setBusy(true);try{await addMetricRecord(tracker,Number(value),new Date(occurred).toISOString(),note);saved()}catch(e){setError(e instanceof Error?e.message:'Unable to save entry')}finally{setBusy(false)}}}>{busy?'Saving…':'Log'}</button></section></div>}
function Rows({title,items,select,unit}:{title:string;items:Tracker[];select:(x:Tracker)=>void;unit:(x:Tracker)=>string}){if(!items.length)return null;return <section className="log-section"><h2>{title}</h2><div className="all-log-list">{items.map(tracker=><button key={tracker.id} onClick={()=>select(tracker)}><span className="tracker-icon"><Activity/></span><div><small>{tracker.module}</small><h3>{tracker.name}</h3><p>{tracker.measurement_type} {unit(tracker)}{tracker.lastRecordedAt?` · last ${new Date(tracker.lastRecordedAt).toLocaleDateString()}`:''}</p></div><span className="row-log">Log</span><ChevronRight/></button>)}</div></section>}
export default function AllLogModule({manage}:{manage:()=>void}){const[trackers,setTrackers]=useState<Tracker[]>([]),[options,setOptions]=useState<TrackerOptions>({areas:[],subcategories:[],units:[]}),[selected,setSelected]=useState<Tracker>(),[loading,setLoading]=useState(true),[error,setError]=useState(''),[query,setQuery]=useState('');const load=async()=>{setLoading(true);try{const data=await loadTrackers();setTrackers(data.trackers.filter(x=>x.status==='active'));setOptions(data.options)}catch(e){setError(e instanceof Error?e.message:'Unable to load Log')}finally{setLoading(false)}};useEffect(()=>{load()},[]);const filtered=useMemo(()=>trackers.filter(x=>`${x.name} ${x.module} ${x.measurement_type}`.toLowerCase().includes(query.trim().toLowerCase())),[trackers,query]),expectedItems=filtered.filter(expected),recent=filtered.filter(x=>!expected(x)&&x.lastRecordedAt).sort((a,b)=>(b.lastRecordedAt??'').localeCompare(a.lastRecordedAt??'')).slice(0,5),frequent=filtered.filter(x=>!expected(x)&&!recent.includes(x)&&x.recentCount>1).sort((a,b)=>b.recentCount-a.recentCount).slice(0,5),available=filtered.filter(x=>!expected(x)&&!recent.includes(x)&&!frequent.includes(x)).sort((a,b)=>a.name.localeCompare(b.name));const unit=(tracker:Tracker)=>options.units.find(x=>x.key===tracker.unit_key)?.symbol||tracker.custom_unit||'';if(loading)return <div className="page-wrap tracker-loading">Loading Log…</div>;return <div className="page-wrap all-log action-log"><header><p className="eyebrow">LOG</p><h1>What happened?</h1></header><label className="log-search"><Search/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search what you can log"/></label>{error&&<p className="goal-error">{error}</p>}{trackers.length?<><Rows title="Expected now" items={expectedItems} select={setSelected} unit={unit}/><Rows title="Recent" items={recent} select={setSelected} unit={unit}/><Rows title="Frequent" items={frequent} select={setSelected} unit={unit}/><Rows title={query?'Results':'Browse'} items={available} select={setSelected} unit={unit}/>{query&&!filtered.length&&<div className="empty-state"><Search/><h2>No matching item</h2><p>Try another name or add something new.</p></div>}</>:<section className="log-get-started"><h2>Get started</h2><p>Add a real item before logging it. Nothing here is preconfigured.</p><div>{['Weight','Sleep','Daily Rating','Alcohol','Exercise','Food'].map(x=><button onClick={manage} key={x}><Plus/> {x}</button>)}</div></section>}<button className="log-manage" onClick={manage}><Plus/> Add / Manage</button>{selected&&<Recorder tracker={selected} unit={unit(selected)} close={()=>setSelected(undefined)} saved={()=>{setSelected(undefined);load()}}/>}</div>}

import{describe,expect,it}from'vitest';import{formatDisplayLabel}from'./displayLabels';
describe('system display labels',()=>{it.each([['breakfast','Breakfast'],['saved_meal','Saved Meal'],['needs_water','Needs Water'],['dormant_woods','Dormant Woods'],['non_negotiable','Non-Negotiable']])('formats %s as %s',(key,label)=>expect(formatDisplayLabel(key)).toBe(label));it('allows locked domain terminology overrides',()=>expect(formatDisplayLabel('completed',{completed:'The Eternal Forest'})).toBe('The Eternal Forest'));it('does not mutate the stable key',()=>{const key='saved_meal';formatDisplayLabel(key);expect(key).toBe('saved_meal')})});

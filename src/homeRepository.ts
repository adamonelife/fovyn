import {supabase} from './supabase';
import {goalOwner} from './goalsRepository';
const fail=(label:string,error:{message:string}|null)=>{if(error)throw new Error(`${label}: ${error.message}`)};
export type HomeHabit={id:string;name:string;tracking_type?:'check'|'count'|'duration';target_value?:number;unit?:string|null;status:'complete'|'failed'|'skipped'|null;frequency_type:'daily'|'specific_days'|'times_per_week'|null;days_of_week:number[]};
export type HomeGoal={id:string;title:string;status:'active'|'dormant'|'completed'|'ended'|'archived';presentation_priority:'primary'|'secondary';area_key:string};
export type HomeData={profile:{first_name:string|null;display_name:string|null;current_climate:string;onboarding_completed_at:string|null};habits:HomeHabit[];goals:HomeGoal[];recentCount:number;configuredCount:number;unresolvedRoundupDate:string|null;unresolvedHabits:HomeHabit[]};
export type HabitResolution={habitId:string;status:'complete'|'failed'|'skipped';value:number|null};
const dateKey=(date:Date)=>`${date.getFullYear()}-${String(date.getMonth()+1).padStart(2,'0')}-${String(date.getDate()).padStart(2,'0')}`;
export function expectedToday(habit:HomeHabit,date=new Date()){if(habit.frequency_type==='daily')return true;if(habit.frequency_type==='specific_days')return habit.days_of_week.includes(date.getDay());return false}

export async function loadHome():Promise<HomeData>{const user=await goalOwner(),now=new Date(),day=dateKey(now),yesterdayDate=new Date(now);yesterdayDate.setDate(yesterdayDate.getDate()-1);const yesterday=dateKey(yesterdayDate),since=new Date(Date.now()-86400000).toISOString();const[profile,goals,habits,recent,configured,yesterdayRecords,yesterdayRoundup]=await Promise.all([supabase.from('profiles').select('first_name,display_name,current_climate,onboarding_completed_at').eq('id',user.id).single(),supabase.from('goals').select('id,title,status,presentation_priority,area_key').eq('owner_id',user.id).order('created_at'),supabase.from('habits').select('id,name,tracking_type,target_value,unit').eq('owner_id',user.id).eq('active',true).is('archived_at',null).lte('start_date',day).or(`ends_on.is.null,ends_on.gte.${yesterday}`),supabase.from('tracking_records').select('id',{count:'exact',head:true}).eq('owner_id',user.id).is('deleted_at',null).gte('occurred_at',since),supabase.from('trackers').select('id',{count:'exact',head:true}).eq('owner_id',user.id).neq('status','archived'),supabase.from('tracking_records').select('id',{count:'exact',head:true}).eq('owner_id',user.id).is('deleted_at',null).gte('occurred_at',`${yesterday}T00:00:00`).lt('occurred_at',`${day}T00:00:00`),supabase.from('daily_roundups').select('id').eq('owner_id',user.id).eq('roundup_date',yesterday).maybeSingle()]);fail('Home profile',profile.error);fail('Home Goals',goals.error);fail('Home habits',habits.error);fail('Recent activity',recent.error);fail('Configured items',configured.error);fail('Yesterday records',yesterdayRecords.error);fail('Yesterday Round-Up',yesterdayRoundup.error);if(!profile.data)throw new Error('Home profile was not found.');const ids=(habits.data??[]).map(x=>x.id);const[schedules,entries]=ids.length?await Promise.all([supabase.from('habit_schedules').select('id,habit_id,frequency_type,days_of_week,effective_from,effective_to').in('habit_id',ids).lte('effective_from',day).or(`effective_to.is.null,effective_to.gte.${yesterday}`),supabase.from('habit_entries').select('habit_id,status,entry_date').in('habit_id',ids).in('entry_date',[day,yesterday])]):[{data:[],error:null},{data:[],error:null}];fail('Home schedules',schedules.error);fail('Home habit entries',entries.error);const makeHabit=(h:{id:string;name:string;tracking_type:'check'|'count'|'duration';target_value:number;unit:string|null},date:string,dateValue:Date):HomeHabit=>{const s=(schedules.data??[]).find(x=>x.habit_id===h.id&&x.effective_from<=date&&(!x.effective_to||x.effective_to>=date)),e=(entries.data??[]).find(x=>x.habit_id===h.id&&x.entry_date===date);return{id:h.id,name:h.name,tracking_type:h.tracking_type,target_value:Number(h.target_value),unit:h.unit,status:(e?.status??null) as HomeHabit['status'],frequency_type:(s?.frequency_type??null) as HomeHabit['frequency_type'],days_of_week:s?.days_of_week??[]}};const todayHabits=(habits.data??[]).map(h=>makeHabit(h,day,now)),yesterdayHabits=(habits.data??[]).map(h=>makeHabit(h,yesterday,yesterdayDate)),unresolvedHabits=yesterdayHabits.filter(h=>expectedToday(h,yesterdayDate)&&!h.status),hadYesterdayActivity=(yesterdayRecords.count??0)>0||(entries.data??[]).some(x=>x.entry_date===yesterday);return{profile:profile.data,goals:(goals.data??[]) as HomeGoal[],recentCount:recent.count??0,configuredCount:configured.count??0,unresolvedRoundupDate:(hadYesterdayActivity||unresolvedHabits.length)&&!yesterdayRoundup.data?yesterday:null,unresolvedHabits,habits:todayHabits}}

export async function completeOnboarding(){const user=await goalOwner();fail('Complete onboarding',(await supabase.from('profiles').update({onboarding_completed_at:new Date().toISOString()}).eq('id',user.id)).error)}
export async function saveRoundup(date:string,mood:'bad'|'ok'|'great',note:string,resolutions:HabitResolution[]){const user=await goalOwner(),now=new Date().toISOString();for(const item of resolutions){fail('Resolve expected item',(await supabase.from('habit_entries').upsert({habit_id:item.habitId,owner_id:user.id,entry_date:date,status:item.status,value:item.value,note:null,updated_at:now},{onConflict:'habit_id,entry_date'})).error)}let noteId:string|null=null;if(note.trim()){const created=await supabase.from('notes').insert({owner_id:user.id,title:'Daily reflection',body:note.trim(),occurred_at:`${date}T20:00:00`}).select('id').single();fail('Save Round-Up note',created.error);noteId=created.data?.id??null}const saved=await supabase.from('daily_roundups').upsert({owner_id:user.id,roundup_date:date,mood,note:null,note_id:noteId,completed_at:now,updated_at:now},{onConflict:'owner_id,roundup_date'});if(saved.error&&noteId)await supabase.from('notes').update({deleted_at:now}).eq('id',noteId).eq('owner_id',user.id);fail('Save Round-Up',saved.error)}

import{describe,expect,it}from'vitest';import{normaliseHobbyAmount,validateHobby}from'./hobbiesRepository';describe('Hobbies validation',()=>{it('keeps blank amounts optional',()=>expect(normaliseHobbyAmount('')).toBeNull());it('normalises leading zero decimals',()=>expect(normaliseHobbyAmount('05.5')).toBe(5.5));it('rejects negative amounts',()=>expect(()=>normaliseHobbyAmount('-1')).toThrow(/zero or more/));it('requires a name and unit',()=>expect(()=>validateHobby({name:'',unit:'minutes'})).toThrow(/name/))});

import {useState} from 'react';import HabitsModule from './HabitsModule';import MetricsModule from './MetricsModule';import TrackModule from './TrackModule';
export default function TrackHub(){const[view,setView]=useState<'habits'|'metrics'|'manage'>('habits');return <><div className="track-hub-switch"><button className={view==='habits'?'active':''} onClick={()=>setView('habits')}>Today & Habits</button><button className={view==='metrics'?'active':''} onClick={()=>setView('metrics')}>Metrics</button><button className={view==='manage'?'active':''} onClick={()=>setView('manage')}>Manage Trackers</button></div>{view==='habits'?<HabitsModule/>:view==='metrics'?<MetricsModule/>:<TrackModule/>}</>}

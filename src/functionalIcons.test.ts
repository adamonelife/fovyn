import{describe,expect,it}from'vitest';import{suggestFunctionalIcon}from'./functionalIcons';
describe('functional icon suggestions',()=>{it.each([['Morning Walk','activity','walking'],['Weekly weigh in','metrics','weight'],['Read','habits','reading'],['Creatine','medication','supplement'],['Piano practice','hobbies','music']])('suggests a stable icon for %s',(name,module,expected)=>expect(suggestFunctionalIcon(name,module as never)).toBe(expected));it('uses the configured measurement when the name is ambiguous',()=>expect(suggestFunctionalIcon('Check in','metrics','money')).toBe('money'))});

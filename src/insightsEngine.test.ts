import{describe,expect,it}from'vitest';import{deriveInsights}from'./insightsEngine';import type{HistoryItem}from'./historyRepository';
const item=(id:string,kind:HistoryItem['kind'],detail:string,date:string):HistoryItem=>({id,kind,title:id,detail,occurredAt:`${date}T12:00:00`,corrected:false,goalNames:[]});
describe('deterministic Insights',()=>{it('returns no claims without evidence',()=>expect(deriveInsights([])).toEqual([]));it('uses real counts and stable tie-breaking',()=>{const result=deriveInsights([item('1','nutrition','Food','2026-08-31'),item('2','sleep','8 hours · Good','2026-08-31'),item('3','sleep','6 hours · Fair','2026-08-30')]);expect(result.find(x=>x.key==='days-present')?.title).toBe('2 days recorded');expect(result.find(x=>x.key==='most-recorded')?.title).toBe('Sleep');expect(result.find(x=>x.key==='sleep-average')?.title).toBe('7.0 hours')});it('excludes N/A from Habit follow-through',()=>{const result=deriveInsights([item('1','habit','Complete','2026-08-31'),item('2','habit','Missed','2026-08-30'),item('3','habit','N/A','2026-08-29')]);expect(result.find(x=>x.key==='habit-resolution')?.title).toBe('50% completed')})});

import{describe,expect,it}from'vitest';import{validateMoney}from'./moneyRepository';describe('money validation',()=>{it('accepts ISO currency',()=>expect(()=>validateMoney({amount:250000,currency:'IDR'})).not.toThrow());it('rejects negative amounts',()=>expect(()=>validateMoney({amount:-1,currency:'GBP'})).toThrow(/negative/));it('rejects invalid currencies',()=>expect(()=>validateMoney({amount:1,currency:'£'})).toThrow(/currency/))});
